# Tasks

## P1. Убрать асинхронную локализацию из первого рендера

- Перенести локализацию из клиентских `fetch` в сборку или встроенный словарь.
- Убрать зависимость первого экрана от `langs.json` и `localImages.json`.
- Сделать так, чтобы нужный язык и картинки были готовы до первого видимого кадра.

Ожидаемый результат:
- Страница рендерится сразу в корректном языке без промежуточного состояния.
- Уходит лишняя асинхронность и снижается риск визуальных фликов.

## P1. Сделать инициализацию Swiper детерминированной

- Убрать гонку между загрузкой `Swiper` и вызовом `new Swiper(...)`.
- Либо отказаться от `async` у внешнего скрипта, либо инициализировать слайдеры только после гарантированной загрузки библиотеки.

Ожидаемый результат:
- Слайдеры инициализируются стабильно на всех загрузках и устройствах.
- Уходит риск случайных ошибок на старте.

## P1. Исправить невалидную интерактивную разметку

- Найти все случаи, где `a` вложен в `button` или наоборот.
- Заменить их на один корректный интерактивный элемент.
- Сохранить текущее визуальное поведение без вложенных controls.

Ожидаемый результат:
- HTML становится валиднее.
- Снижается риск странного поведения в браузерах и проблем с доступностью.

## P1. Добавить защиту JS-инициализации от отсутствующих элементов

- Проверять существование `select`, контейнеров слайдеров и других DOM-узлов перед инициализацией.
- Изолировать инициализацию по блокам, чтобы одна отсутствующая нода не ломала весь скрипт.

Ожидаемый результат:
- Скрипт становится устойчивее к изменениям разметки.
- Проще добавлять новые страницы без случайных runtime-ошибок.

## P2. Убрать дублирование шаблонов страниц

- Определить единый source of truth для страницы.
- Использовать `src/pages/*.html` и `src/components/*.html` по назначению, без ручного редактирования сгенерированного `src/index.html`.
- При необходимости разнести header/footer/секции в компоненты.

Ожидаемый результат:
- Меньше ручного дублирования.
- Изменения вносятся в одном месте, а не в нескольких копиях.

## P2. Усилить безопасность внешних ссылок

- Добавить `rel="noopener noreferrer"` ко всем ссылкам с `target="_blank"`.
- Проверить, что это сделано одинаково по всей странице.

Ожидаемый результат:
- Корректное и безопасное открытие внешних ссылок.

## P2. Подтянуть базовую доступность

- Добавить `aria-label` для кнопок без текста.
- Проверить `alt` у изображений: декоративные отметить пустым `alt`, смысловые описать точнее.
- Добавить понятную подпись или `aria-label` для селекта языка.

Ожидаемый результат:
- Интерфейс становится понятнее для screen reader.
- Снижается количество базовых accessibility-проблем.

## P3. Причесать Gulp-сборку и watcher'ы

- Проверить watcher для картинок и заменить путь на рекурсивный, если нужно.
- Пересмотреть структуру тасков, чтобы dev/build-процессы были предсказуемее.
- Убедиться, что генерируемые файлы не используются как исходники.

Ожидаемый результат:
- Сборка работает стабильнее.
- Меньше скрытых проблем из-за неверных watch-путей и смешения source/build-артефактов.

## P3. Дописать README

- Добавить инструкции по запуску dev-режима и сборки.
- Описать структуру проекта: где source, где generated-файлы, что редактировать руками.
- Кратко зафиксировать принятые правила работы с проектом.

Ожидаемый результат:
- Проект проще поддерживать и передавать другому разработчику.
